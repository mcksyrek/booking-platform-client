export enum Routes {
  Offers = '/offers',
  All = '/all',
  New = '/new',
  Auth = '/auth',
  Login = '/login',
  Registration = '/registration',
  Reservations = '/reservations',
  Client = '/client',
  Provider = '/provider',
}
